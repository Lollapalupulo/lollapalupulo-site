// Exemplo: Menu Mobile (para versão responsiva)
document.addEventListener('DOMContentLoaded', function() {
  console.log('Site carregado!');
  // Adicione interações aqui (ex.: carrossel, accordion para programação)
});
